//
//  SFRubyObserver.hpp
//  gold-miner-vegas
//
//  Created by eps on 3/2/18.
//

#ifndef SF_RUBY_OBSERVER_HPP
#define SF_RUBY_OBSERVER_HPP

#include "SFForward.hpp"

#include <boost/shared_ptr.hpp>
/*

namespace sf {
class RubyObserver {
private:
    using Self = RubyObserver;

public:
    using Callback = std::function<void(int amount)>;

    explicit RubyObserver(const Callback& callback);

    ~RubyObserver();

protected:
    void onUserInfoRequest(
        const boost::shared_ptr<Sfs2X::Entities::Data::ISFSObject>& object);
    void onSetRubyRequest(
        const boost::shared_ptr<Sfs2X::Entities::Data::ISFSObject>& object);

private:
    Callback callback_;

    std::shared_ptr<ExtensionResponseListener> extensionListener_;
};
} // namespace sf
 */

#endif /* SF_RUBY_OBSERVER_HPP */
